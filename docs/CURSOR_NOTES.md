# Notas para Cursor (IA)

Cuando abras el repo en Cursor, puedes pedirle:
- "Genera tests unitarios para backend/src/controllers/productsController.js"
- "Conecta el frontend a Firebase: actualiza frontend/src/firebase.js con las variables de entorno"
- "Crea validaciones para createProduct usando express-validator"
- "Genera un componente AdminProductList que consuma /api/products"
- "Crea scripts npm para seeding y despliegue"

Cursor puede editar archivos en el repo y generar commits; recuerda revisar antes de hacer push.
