# Firebase E-commerce Starter

Contenido:
- frontend: React app skeleton with Firebase client SDK
- backend: Node.js + Express with firebase-admin examples (CRUD)
- docs: Firebase rules and setup notes

IMPORTANTE: No subir `serviceAccountKey.json` a GitHub. Añádelo a `.gitignore`.
